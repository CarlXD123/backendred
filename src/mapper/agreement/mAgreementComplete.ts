export interface mAgreementComplete {
    id: number,
    name: string,
    address: string,
    ruc: string,
    email: string,
    tlfNumber: string,
    description: string,
    TypeAgreementId: number
}